# Core runtime dependencies for LaunchPoint (Controller-Origin Finder).
# All pinned to floors known to work on CPython 3.10-3.12.
# Install with:  pip install -r requirements.txt
# or use the one-click installer:  install_dependencies.bat
numpy>=1.24
scipy>=1.10
rasterio>=1.3
pyproj>=3.5
shapely>=2.0
geopandas>=0.13
numba>=0.58
requests>=2.28

# Test/dev tooling (used by tests/ and CI)
pytest>=7.4
pytest-cov>=4.1

# Optional GPU acceleration for the viewshed kernel (Phase 5).
# Requires an NVIDIA GPU (compute capability >= 5.0). The [cu12] extra bundles
# the CUDA 12 toolkit wheels (NVVM + cudart) the kernel JITs against, so no
# system-wide CUDA install is needed. Uncomment to enable:
# numba-cuda[cu12]>=0.30
